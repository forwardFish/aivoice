<view class="audio-shell {{compact ? 'audio-compact' : ''}} {{bubble ? 'audio-bubble' : ''}}" bindtap="toggle">
  <view class="play-button {{playing ? 'is-playing' : ''}}">
    <view wx:if="{{!playing}}" class="play-triangle"></view>
    <view wx:else class="pause-bars"><text class="pause-bar"></text><text class="pause-bar"></text></view>
  </view>
  <view class="audio-main">
    <view wx:if="{{label || tag}}" class="audio-meta">
      <text class="audio-label">{{label}}</text>
      <text wx:if="{{tag}}" class="audio-tag">{{tag}}</text>
    </view>
    <view class="wave-row">
      <view class="wave-bars">
        <view wx:for="{{bars}}" wx:key="index" class="wave-bar" style="height: {{item}}rpx;"></view>
        <view class="wave-progress" style="width: {{progress}}%;"></view>
      </view>
      <text class="audio-time">{{durationOnly ? durationLabel : currentText}}</text>
    </view>
  </view>
</view>

.state-shell {
  min-height: 560rpx;
  padding: 90rpx 40rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.state-compact {
  min-height: 300rpx;
  padding: 48rpx 28rpx;
}

.state-visual {
  width: 126rpx;
  height: 126rpx;
  border-radius: 50%;
  margin-bottom: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 58rpx;
  font-weight: 700;
  box-shadow: 0 18rpx 44rpx rgba(101, 82, 245, 0.16);
}

.loading-visual,
.empty-visual {
  position: relative;
  background: radial-gradient(circle at 30% 25%, #ffffff, #eae7ff 58%, #cfdeff);
}

.loading-ring {
  position: absolute;
  top: 4rpx;
  right: 4rpx;
  bottom: 4rpx;
  left: 4rpx;
  border-radius: 50%;
  border: 5rpx solid rgba(101, 82, 245, 0.14);
  border-top-color: #6552f5;
  animation: state-spin 1s linear infinite;
}

.loading-wave,
.empty-wave {
  display: flex;
  align-items: center;
  gap: 5rpx;
}
.empty-wave-image { width:84rpx; height:48rpx; }

.state-wave-bar {
  width: 6rpx;
  border-radius: 999rpx;
  background: #6552f5;
}

.state-wave-bar:nth-child(1) { height:22rpx; }
.state-wave-bar:nth-child(2) { height:36rpx; }
.state-wave-bar:nth-child(3) { height:52rpx; }
.state-wave-bar:nth-child(4) { height:36rpx; }
.state-wave-bar:nth-child(5) { height:22rpx; }

.error-visual {
  color: #ffffff;
  background: linear-gradient(135deg, #ea7d84, #cf525d);
}

.success-visual {
  color: #ffffff;
  background: linear-gradient(135deg, #62ca84, #43a968);
}

.state-title {
  font-size: 33rpx;
  line-height: 1.4;
  font-weight: 650;
  color: #11172d;
}

.state-description {
  max-width: 560rpx;
  margin-top: 14rpx;
  font-size: 25rpx;
  line-height: 1.65;
  color: #737b94;
}

.state-action-shell { width:280rpx; margin-top:34rpx; }

@keyframes state-spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
